package ex3;

import java.util.Iterator;

import ex3.MulticonjuntoSet.Wrapper;

public class MulticonjuntoAbstrato<T> {
	class Wrapper {
		T element;
		int multiplicity = 0;
		
		public Wrapper (T e, int m) {
			this.element = e;
			this.multiplicity = m;
		}
	}
	
	@Override
	public boolean equals(Object other) {
		if (this.getClass().isAssignableFrom(other.getClass())) {
			Iterator i = ((MulticonjuntoSet)other).iterator();
			while(i.hasNext()) {
				Wrapper w = (Wrapper)i.next();
				if (getMultiplicity(w.element) != w.multiplicity) return false;
			}
			return true;
		}
		return false;
	}
}
