package ex3;
import java.util.*;

public class MulticonjuntoSet<T> extends HashSet<MulticonjuntoSet<T>.Wrapper> {
	class Wrapper {
		T element;
		int multiplicity = 0;
		
		public Wrapper (T e, int m) {
			this.element = e;
			this.multiplicity = m;
		}
	}
	
	@Override
	public boolean contains(Object e) {
		Iterator<MulticonjuntoSet<T>.Wrapper> i = iterator();
		while(i.hasNext()) {
			Wrapper w = (Wrapper)i.next();
			if (w.element.equals(e)) return true;
		}
		
		return false;
	}
	
	public boolean add(T e) {
		if ()
		return super.add(e);
    }
}
