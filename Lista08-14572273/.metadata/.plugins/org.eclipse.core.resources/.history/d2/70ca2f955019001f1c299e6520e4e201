package ex2;

import javax.swing.*;

import org.junit.jupiter.api.*;

import ex1.FatorialInterface;

public class TestFatorialWindow {
	static JFrame frame;
	
	@BeforeAll
	public static void setup() throws NoSuchFieldException, SecurityException, IllegalArgumentException, IllegalAccessException {
		frame = new FatorialInterface();
		frame.setVisible(true);
		
		(JTextField)TestFatorialWindow.class.getField("textField").get(frame);
		(JButton)TestFatorialWindow.class.getField("button").get(frame);
		(JLabel)TestFatorialWindow.class.getField("resultLabel").get(frame);
	}
	
	@AfterAll
	public static void cleanUp() {
		frame.setVisible(false);
		
	}
	
	@Test
	public void test() {
		
	}
	
	
}
